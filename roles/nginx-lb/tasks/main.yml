--- 
- name: Install nginx
  become: true
  apt:  name=nginx update_cache=yes state=latest
    
- name: restart and enable nginx
  become: true
  ansible.builtin.service:
   name: nginx 
   state: started 
   enabled: yes

# - name: restart nginx
#   become: true
#   service: name=nginx state=restarted

- name: copy load_balancer.conf file
  become: true
  command: "cp -r /home/ubuntu/ansible-config-mgt/downloads/load_balancer.conf /etc/nginx/sites-available/"

# - name: linking load_balancer.conf file
#   become: true
#   command:
#     -  "cd /etc/nginx/sites-enabled/"
#     -  "ln -s ../sites-available/load_balancer.conf ."
  
# - name: restart nginx
#   become: true
#   service: name=nginx state=restarted












# - name: choose Load Balancer
#   vars:
#     enable_apache_lb: false
#     enable_nginx_lb: false
#     load_balancer_is_required: false

